# get lenth from the user
length = int(input("Enter the length of the Square: "))
print(f"Length: {length}")

# calculate and store area in variable using the formula
# length * length
area = length * length
# area = int(length) ^ 2

# print the calculated area to the terminal
print(f"Area: {area}")